{
  "app": "npx ts-node --project tsconfig.json bin/app.ts",
  "context": {
    "@aws-cdk/core:enablePartitionNaming": true,
    "@aws-cdk/aws-apigateway:usagePlanKeyOrderInsensitiveId": true,
    "@aws-cdk/core:stackRelativeExports": true,
    "@aws-cdk/aws-ecr-assets:dockerIgnoreSupport": true,
    "@aws-cdk/aws-secretsmanager:parseOwnedSecretName": true,
    "@aws-cdk/aws-kms:defaultKeyPolicies": true,
    "@aws-cdk/aws-s3:grantWriteWithoutAcl": true,
    "env:comment": "REQUIRED: specify --context env=LOCAL_MOCK or --context env=PERSONAL_AWS_DEV or --context env=COMPETITION_AWS",
    "env:LOCAL_MOCK": {
      "profile": "LOCAL_MOCK",
      "description": "Local development without AWS credentials",
      "credentials": "none"
    },
    "env:PERSONAL_AWS_DEV": {
      "profile": "PERSONAL_AWS_DEV",
      "description": "Personal AWS dev account — shared architecture with COMPETITION_AWS",
      "requiresCredentials": true
    },
    "env:COMPETITION_AWS": {
      "profile": "COMPETITION_AWS",
      "description": "Official competition AWS environment — shared architecture with PERSONAL_AWS_DEV",
      "requiresCredentials": true
    }
  }
}
